/**
 * Address model
 */

export class Address {
    id: number;
    
    // Street Address
    street: string;
    
    // City
    city: string;

    // State
    state: string;

    // Zip
    zip: string;
}